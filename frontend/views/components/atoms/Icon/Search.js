import React from "react";
import Icon from "./_base";
import SearchIcon from "frontend/icons/search.svg";
 
const Search = props => {
  return <Icon icon={SearchIcon} {...props} />;
};

export default Search