import React from "react";
import Icon from "./_base";
import CloseIcon from "frontend/icons/close.svg";
 
const Close = props => {
  return <Icon icon={CloseIcon} {...props} />;
};

export default Close