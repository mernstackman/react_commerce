import React from "react";
import Icon from "./_base";
import CalendarIcon from "frontend/icons/calendar.svg";
 
const Calendar = props => {
  return <Icon icon={CalendarIcon} {...props} />;
};

export default Calendar