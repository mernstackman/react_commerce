import React from "react";
import Icon from "./_base";
import RightIcon from "frontend/icons/right.svg";
 
const Right = props => {
  return <Icon icon={RightIcon} {...props} />;
};

export default Right