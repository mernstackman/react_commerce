import React from "react";
import Icon from "./_base";
import EmailIcon from "frontend/icons/email.svg";
 
const Email = props => {
  return <Icon icon={EmailIcon} {...props} />;
};

export default Email