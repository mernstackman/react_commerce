import React from "react";
import Icon from "./_base";
import BackRedIcon from "frontend/icons/back-red.svg";
 
const BackRed = props => {
  return <Icon icon={BackRedIcon} {...props} />;
};

export default BackRed